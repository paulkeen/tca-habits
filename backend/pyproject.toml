[project]
name = "tca-habits-backend"
version = "0.1.0"
description = "TCA Habits — basic habit tracker backend (Section 1)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.29",
    "sqlalchemy>=2.0",
    "pydantic>=2.0",
]

[tool.uv]
package = false
